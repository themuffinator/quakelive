name: UI Freeze Guard

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  ui-freeze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Enforce retail UI immutability
        run: tools/enforce_ui_freeze.sh
