# Renderer CVar matrix (Quake Live parity)

This matrix summarizes renderer CVars whose defaults and ranges are mirrored from the Quake Live HLIL dumps under `references/hlil/quakelive/`. The table lists the in-source defaults, enforced clamps, observed HLIL constraints, and the code paths expected to respect each setting.

| CVar | Default (source) | Clamp/Range | Usage / Side effects | HLIL reference |
| --- | --- | --- | --- | --- |
| `r_picmip` | Archive/latched/protected/cloud `0` texture LOD bias.【F:src/code/renderer/tr_init.c†L1114-L1117】 | Integral `0..16`.【F:src/code/renderer/tr_init.c†L218-L266】 | Texture scaling when uploading mipmapped images.【F:src/code/renderer/tr_image.c†L549-L550】 | Retail Ghidra registers default `0` with flags `0x80821`, and the source clamp keeps the accepted range at `0..16`. |
| `r_displayRefresh` | Latched `0` (meaning driver default).【F:src/code/renderer/tr_init.c†L1153-L1156】 | Integral `0..240` Hz.【F:src/code/renderer/tr_init.c†L223-L227】 | Applied to Win32 display mode if non-zero.【F:src/code/win32/win_glimp.c†L808-L810】 | HLIL clamps to `0..240` immediately after registration.【7322d6†L33-L38】 |
| `r_znear` | Cheat `1`.【F:src/code/renderer/tr_init.c†L1165-L1167】 | Integral `0.001..200`.【F:src/code/renderer/tr_init.c†L228-L232】 | Drives projection near plane distance.【F:src/code/renderer/tr_main.c†L460-L460】 | Registered without explicit HLIL default clamp beyond cheat range. |
| `r_ambientScale` | Bounded protected/VM-created/cloud `10`.【F:src/code/renderer/tr_init.c†L1189-L1192】 | Integral `1..100`.【F:src/code/renderer/tr_init.c†L233-L236】 | Scales ambient contribution after light-grid renormalization and before directed scaling.【F:src/code/renderer/tr_light.c†L116-L238】 | HLIL embeds default `10`, minimum `1`, maximum `100`, and flags `0x81800`; Ghidra shows the cvar value multiplied into all three ambient-light channels.【7f9cdb†L7-L16】【20c8cb†L73-L77】 |
| `r_directedScale` | Cheat `1`.【F:src/code/renderer/tr_init.c†L1189-L1192】 | No source-side range clamp beyond cvar parsing. | Scales directed-light contribution after light-grid renormalization and after ambient scaling.【F:src/code/renderer/tr_light.c†L116-L239】 | HLIL registers the cvar with default `1` and flags `0x200`; Ghidra shows the cvar value multiplied into all three directed-light channels. |
| `r_aspectRatio` | Latched `0` (auto).【F:src/code/renderer/tr_init.c†L1136-L1138】 | Integral `0..3` presets.【F:src/code/renderer/tr_init.c†L238-L241】 | Presets 1–3 drive Hor+ FOV scaling in cgame.【F:src/code/cgame/cg_view.c†L34-L98】 | Registered as latched CVar with HLIL string table for presets.【7f9cdb†L7-L16】【dfa1ab†L66-L70】 |
| `r_mode` / `modelist` | Latched archive/cloud default `-2`; `modelist` is a command, not a CVar. | `-2` resolves the native desktop mode in the Win32 host, `-1` uses custom/windowed dimensions, and `0..27` indexes the retail table. | `modelist` prints the 28 retail descriptions, table-backed modes publish their stored aspect preset, and custom/native modes classify aspect with retail thresholds before updating `r_aspectRatio`. | HLIL registers `r_mode` with flags `0x80021`, wires `modelist` to `sub_448160`, and stores the packed 28-entry mode table plus native-desktop wrapper. |
| `r_customWidth` / `r_customHeight` | Latched archive/cloud defaults `1600` / `1024`. | Used when fullscreen `r_mode == -1`; source keeps the existing positive-value repair guard. | Fullscreen custom modes feed `R_GetModeInfo`, which now returns the retail aspect preset instead of deriving only a float window aspect. | HLIL registers the capitalized names with flags `0x80021` and defaults `1600` / `1024`. |
| `r_textureBits` | Latched archive/cloud default `32`. | No extra clamp beyond CVar parsing. | Printed by `gfxinfo` and consumed by texture upload code to select 16-bit or 32-bit internal texture formats. | HLIL/Ghidra register the capitalized Quake Live name with flags `0x80021` and default `32`. |
| `r_picmip` / `r_roundImagesDown` / `r_colorMipLevels` / `r_detailtextures` / `r_simpleMipMaps` / `r_vertexLight` / `r_overBrightBits` / `r_mapOverBrightBits` / `r_mapOverBrightCap` / `r_gamma` | Retail image/lighting defaults: `0`, `1`, `0`, `1`, `1`, `0`, `1`, `2`, `255`, `1`. | `r_picmip` is clamped to `0..16`; `r_mapOverBrightCap` is bounded `0..255`; remaining entries have no extra source clamp beyond cvar parsing and existing gamma repair. | Drive mip scaling, image rounding, mip debug coloring, detail stage selection, mipmap generation, vertex-lighting fallbacks, texture/postprocess overbright, BSP lightmap overbright shifting/capping, and gamma correction. | Ghidra registers the ten retail contracts in the renderer block; `r_picmip` carries protected/cloud flags, `r_mapOverBrightCap` is a bounded VM-created/cloud latch, and `r_gamma` uses the cross-platform retail default `1`. |
| `r_uifullscreen` / `r_subdivisions` / `r_smp` / `r_ignoreFastPath` / `r_ignoreGLErrors` / `r_ignore` / `r_intensity` / `r_saveFontData` / `r_maxPolys` / `r_maxPolyVerts` | Retail defaults: `0`, `4`, `0`, `1`, `1`, `1`, `1`, `0`, `600`, `3000`. | `r_subdivisions` and `r_smp` are ROM; `r_ignoreFastPath` and `r_intensity` are archive+latch; `r_ignoreGLErrors` is archive; `r_ignore` is cheat; font save and max-poly cvars are unflagged. | Drive UI-fullscreen vertex-lightmap behavior, curve subdivision, SMP command routing, tess fast-path suppression, GL error polling, debug surface offsets, intensity mapping, font atlas/cache dumps, and dynamic poly-buffer sizing. | Ghidra registers the retail lowercase/camel-case names, including `r_uifullscreen`, `r_maxPolys`, and `r_maxPolyVerts`; mixed-case `Cvar_Set("r_uiFullScreen", ...)` remains compatible through case-insensitive cvar lookup. |
| `r_fastSkyColor` / `r_forceMergeEntities` / `r_gl_vendor` / `r_gl_renderer` / `r_gl_reserved` / `r_stereo` / `r_drawSkyFloor` / `r_noFastRestart` / `r_skipSmallBatches` / `r_teleporterFlash` | Retail defaults: `0x000000`, `0`, `None`, `None`, `0`, `0`, `1`, `0`, `0`, `1`. | Retail flags are archive/protected/cloud for fast-sky color and teleporter flash, cheat for forced entity merge and small-batch skipping, ROM/protected for GL status cvars, archive+latch for stereo, archive/cloud for sky floor, and archive for fast-restart suppression. | Drive fast-sky clear color, forced cross-entity draw batching, GL vendor/renderer/occlusion status publication, stereo pixel format selection, bottom sky/cloud side drawing, fast vid-restart permission, small-batch diagnostic drops, and teleporter flash overlay gating. | Ghidra registers all ten in the retail renderer/platform blocks; HLIL also shows `r_drawSkyFloor` gating side `5` and `r_gl_reserved` being set when GL occlusion-query support is complete. |
| `r_lastValidRenderer` / `r_allowSoftwareGL` / `r_inGameVideo` / `r_contrast` / `r_railWidth` / `r_railCoreWidth` / `r_railSegmentLength` / `r_portalOnly` / `r_speeds` / `r_textureMode` | Retail defaults: `(uninitialized)`, `0`, `1`, `1.0`, `16`, `6`, `32`, `0`, `0`, `GL_LINEAR_MIPMAP_LINEAR`. | Retail flags are archive for last-valid renderer and in-game video, latch for software GL, archive/cloud for contrast, rail geometry, and texture mode, and cheat for portal-only and performance-spew diagnostics. | Drive GL hardware-change detection, software pixel-format acceptance, wall-cinematic playback and UI toggles, color-correct contrast, rail ring/core dimensions, portal-only debugging, renderer performance counters, texture filtering, and hardware-specific texture-mode repair. | HLIL/Ghidra register the same defaults/flags; Win32 HLIL also shows `r_lastValidRenderer` being updated after GL detection, while renderer/client source consumers keep the observed functionality wired. |
| `r_glDriver` / `r_allowExtensions` / `r_ext_compressed_textures` / `r_ext_gamma_control` / `r_ext_multitexture` / `r_ext_compiled_vertex_array` / `r_ext_texture_env_add` / `r_ignoreHWGamma` / `r_floatingPointFBOs` / `r_maskMinidriver` | Retail startup/extension defaults: `opengl32`, `1`, `0`, `1`, `1`, `1`, `1`, `0`, `0`, `0`. | Latched archive/cloud flags mirror the retail registration block; no extra source clamp beyond cvar parsing. | Drive GL driver loading, global extension opt-out, S3TC, gamma control, multitexture, compiled vertex arrays, texture-env-add availability, hardware gamma bypass, floating-point FBO format selection, and minidriver masking. | HLIL/Ghidra register `r_ext_texture_env_add` with default `1` and flags `0x80021`; the old platform-specific `0` branch was not retail parity. |
| `r_lodBias` / `r_lodScale` | Bounded defaults `-2` / `10`. | Integral `-2..2` and `1..50`. | `r_lodBias` offsets selected MD3 LOD, while `r_lodScale` scales LOD projection error before model LOD selection. | HLIL registers both with bounded-cvar metadata: `r_lodBias` flags `0x81801`, `r_lodScale` flags `0x81000`. |
| `r_enablePostProcess` / `r_enableBloom` / `r_enableColorCorrect` / `r_postProcessActive` / `r_bloomActive` / `r_colorCorrectActive` / `r_bloomPasses` / `r_bloomIntensity` / `r_bloomBrightThreshold` / `r_bloomBlurScale` | Retail defaults: `1`, `1`, `1`, `0`, `0`, `0`, `1`, `0.5`, `0.25`, `0.0`; the three enable gates are archive+latch+cloud. | `r_enablePostProcess` now stays enabled by default like retail; bloom/color-correct enable flags remain archive+latch+cloud; active mirrors are ROM/temp, with cloud additionally on `r_bloomActive`; bounded bloom flags match retail (`r_bloomPasses` also latch/protected/discrete/cloud). Ranges are postprocess `0..1`, bloom mode `0..2`, color-correct `0..1`, passes `1..2`, intensity `0..10`, threshold `0..1`, blur scale `1..2`. | The shader-backed postprocess gate lets color correction own final `r_gamma` output on capable GPUs, which keeps desktop/PrintScreen captures aligned with retail and leaves Win32 hardware gamma as the fallback when postprocess or color-correct is disabled/unavailable. `r_enableBloom` is the retail bloom mode selector (`2` enables the quarter-resolution chain); `r_bloomIntensity` and `r_bloomBrightThreshold` feed shader uniforms. | Ghidra registers all ten in the retail renderer block; HLIL also shows `r_postProcessActive`, `r_bloomActive`, and `r_colorCorrectActive` being set as active-state mirrors. |
| `r_clear` / `r_drawBuffer` / `r_logFile` / `r_lightmap` / `r_measureOverdraw` / `r_offsetfactor` / `r_offsetunits` / `r_primitives` / `r_swapInterval` / `r_verbose` | Retail defaults: `0`, `GL_BACK`, `0`, `0`, `0`, `-1`, `-2`, `0`, `0`, `0`. | Cheat flags for clear, draw buffer, logging, overdraw, polygon offset, and verbose PFD diagnostics; unflagged `r_lightmap`; archive `r_primitives`; archive/cloud `r_swapInterval`. | Drive debug clears, front/back draw buffer and swap suppression, counted GL logging, lightmap-only/debug rendering, stencil overdraw measurement, polygon offset fill bias, primitive submission mode, WGL swap interval, and verbose pixel-format rejection prints. | Ghidra/HLIL register the same defaults/flags; HLIL also shows retail disabling `r_measureOverdraw` on invalid stencil/shadow combinations and decrementing `r_logFile` during GL logging. |
| `r_bloomPasses` | Bounded archive/latched/protected/VM-created/discrete/cloud `1`.【F:src/code/renderer/tr_init.c†L1103-L1107】 | Integral `1..2`.【F:src/code/renderer/tr_init.c†L243-L246】 | Registered and clamped for retail parity; no backend read is observed in the committed HLIL/source, where `r_enableBloom` selects the extra bloom stage. | HLIL/Ghidra register default `1`, min `1`, max `2`, and flags `0x82821`. |
| `r_bloomIntensity`, `r_bloomBrightThreshold`, `r_bloomBlurScale`, `r_bloomBlurRadius`, `r_bloomBlurFalloff`, `r_bloomSaturation`, `r_bloomSceneIntensity`, `r_bloomSceneSaturation` | Retail bounded defaults mirror HLIL (0.5, 0.25, 0, 5, 0, 0.8, 1, 1).【F:src/code/renderer/tr_init.c†L1256-L1263】 | Clamped to HLIL-observed ranges: intensity/scene* `0..10`, threshold `0..1`, blur scale `1..2`, radius `1..12`, falloff `0.75..1.0`, saturations `0..10`.【F:src/code/renderer/tr_init.c†L286-L313】 | Intensity, threshold, and saturation controls feed the shader-backed bloom combine path; the blur scale/radius/falloff controls are retained as retail bounded cvars with no observed backend consumer. | Registered with explicit min/max pointers in HLIL data tables; searches show blur scale/radius/falloff references only at registration/string-data sites. |
| `r_contrast` | Archive/cloud `1.0`.【F:src/code/renderer/tr_init.c†L1336-L1336】 | No extra clamp beyond float parsing; mirrors the retail registration surface recovered from Ghidra companion output.【F:src/code/renderer/tr_init.c†L1336-L1336】 | Drives the color-correct shader through `p_contrast` alongside `r_gamma` and `r_overBrightBits`.【F:src/code/renderer/tr_backend.c†L1288-L1288】【F:src/code/renderer/tr_backend.c†L1912-L1919】 | Present in the retail post-process registration block recovered from companion decompilation. |
| `r_windowedMode` / `r_windowedWidth` / `r_windowedHeight` | Latched defaults `12`, `1600`, `1024`.【F:src/code/renderer/tr_init.c†L1131-L1135】 | No source-side reset or clamp beyond cvar integer parsing; `mode == -1` forwards the windowed width/height pair directly.【F:src/code/renderer/tr_init.c†L551-L571】 | Used both for retained windowed-mode selection and for the retail Win32 live client-rect sync helper, which rewrites them to `-1` / live width / live height before queuing `vid_restart fast` on resize, restore, or maximize transitions.【F:src/code/renderer/tr_init.c†L1081-L1160】【F:src/code/win32/win_wndproc.c†L74-L95】【F:src/code/win32/win_wndproc.c†L519-L568】 | HLIL registers with matching defaults and latch flags, and the retail Win32 host updates the same trio from the live client rect via `sub_4F1290`. |
| `r_teleporterFlash` | Archive/protected/cloud `1`.【F:src/code/renderer/tr_init.c†L1172-L1174】 | Integral `0..1`.【F:src/code/renderer/tr_init.c†L288-L292】 | Enables the red flash overlay when teleporting. | HLIL/Ghidra register flags `0x80801`. |
| `r_skipLargeBatches` / `r_skipSmallBatches` | Cheat `0`.【F:src/code/renderer/tr_init.c†L1214-L1216】 | Integral `0..1`.【F:src/code/renderer/tr_init.c†L293-L301】 | Diagnostic toggles intended to drop specific draw batches during backend submission.【F:src/code/renderer/tr_init.c†L1081-L1160】 | Registered as cheat toggles in HLIL loader. |
| `r_fullbright` / `r_singleShader` / `r_printShaders` / `r_showImages` / `r_showSmp` / `r_skipBackEnd` / `r_skipLargeBatches` / `r_debuglight` / `r_debugFontAtlas` / `r_debugAds` | Retail diagnostic defaults are `0`; `r_debuglight`, `r_debugFontAtlas`, `r_debugAds`, and `r_showImages` are temporary, `r_printShaders` is unflagged, and the rest are cheat or latched-cheat/latched-archive as observed. | `r_skipLargeBatches` is clamped to `0..1`; the rest have no extra clamp beyond cvar parsing. | Drive fullbright/single-shader BSP loading, shader print diagnostics, image/font/ad overlays, SMP stall prints, backend skipping, large-batch dropping, and light-grid logging. `RE_EndRegistration` keeps the retail texture-residency touch as a non-visual bind walk so the visible `r_showImages` cache atlas only appears when the debug cvar is active. | Ghidra/HLIL register all ten in the retail `R_Register` block; `r_debugAds` specifically uses flag `0x100`, not cheat. |
| `r_noCurves` / `r_noCull` / `r_noPortals` / `r_noBind` | Cheat `0`. | No extra clamp beyond cvar integer parsing. | Skip curve surfaces, disable culling, suppress portal views, or force texture binding to the diagnostic image through the existing renderer globals. | Retail registers the Quake Live camel-case names with flags `0x200`; the source globals intentionally keep the historical internal variable names. |
| `r_showCluster` / `r_showTris` / `r_showSky` / `r_showNormals` / `r_debugShaderIndex` / `r_debugSort` / `r_debugSortExcept` / `r_debugSurface` | Cheat `0`. | No extra clamp beyond cvar integer parsing. | Drive renderer diagnostics for cluster printing, wireframe triangles, sky depth forcing, normal visualization, shader-index wireframe highlighting, shader-sort filtering with one exempt sort, and collision debug surface drawing. | Retail registers each diagnostic CVar with default `0` and flags `0x200`; wiring remains in the world, sky, shade, and debug-surface paths. Source-only Q3/legacy platform leftovers (`r_customaspect`, `r_dlightBacks`, `r_previousglDriver`, `r_enablerender`, `r_appleTransformHint`, `r_minDisplayRefresh`, `r_maxDisplayRefresh`) are not part of the Quake Live retail `r_*` surface. |
| `r_noFastRestart` | Archive `0`.【F:src/code/renderer/tr_init.c†L1159-L1162】 | Integral `0..1`.【F:src/code/renderer/tr_init.c†L303-L306】 | Prevents `vid_restart` fast path; enforced alongside other latched video CVars.【F:src/code/renderer/tr_init.c†L1081-L1160】 | Registered as archive toggle in HLIL block.【7322d6†L89-L97】 |
| `r_drawSkyFloor` | Archive/cloud `1`.【F:src/code/renderer/tr_init.c†L1168-L1170】 | Integral `0..1`.【F:src/code/renderer/tr_init.c†L308-L311】 | Controls whether the bottom sky/cloud side is generated. | HLIL/Ghidra register flags `0x80001` and gate side `5` on the cvar. |

## Menu validation

The advanced options menu presets clamp bloom CVars to values that fall within the enforced ranges (e.g., thresholds `0.10–0.25`, saturation up to `1.25`).【F:assets/quakelive/baseq3/ui/ingame_options_advanced.menu†L8011-L8058】 No menu entries set values outside the documented limits, so existing UI presets remain valid after the clamping changes.
